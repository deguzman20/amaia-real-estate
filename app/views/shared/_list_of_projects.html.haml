%ul.list-group
  %li.list-group-item.active{ :style => "background-color:#64bf5b" } 
    %center
      LIST OF PROJECTS
- available_locations.each do |available_location|
  - if location(available_location).present?
    %li.list-group-item
      %span{ :style => "color:#6dbb65" }
        = "AMAIA #{location(available_location)}"
      %ul.list-group
      - units(available_location).each do |unit|
        %li.list-group-item.d-flex.justify-content-between.align-items-center
          = link_to "Amaia #{unit.try(:type).try(:name)}  #{unit.try(:specific_location)}", unit_path(unit.id), style: "color:black"
          %span.badge.badge-pill{ :style => "background-color:black" } >
